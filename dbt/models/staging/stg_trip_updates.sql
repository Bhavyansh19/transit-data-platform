-- Week 4: clean + type the raw trip_updates data landed by src/ingest_realtime_gtfs.py
-- Fill in once raw data is actually landing in the warehouse.

select
    -- trip_id,
    -- station_id,
    -- ...
    1 as placeholder
